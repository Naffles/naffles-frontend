import { TextInput } from "./TextInput";

const meta = {
  title: "Components/Inputs/TextInput",
  component: TextInput,
};

export default meta;

export const Basic = {
  args: {
    label: "Text input",
    placeholder: "test text",
  },
};
